<section class="section">
  <div class="page-width rich-text__inner">
    <h1>{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{
  "name": "Main page"
}
{% endschema %}
